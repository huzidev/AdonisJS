import type { HttpContextContract } from '@ioc:Adonis/Core/HttpContext';

export default class CommentsController {
    public async addComment({ auth }: HttpContextContract) {
        try {
            
        } catch (e) {
            throw e;
        }
    }
}
